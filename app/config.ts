const config = {
  siteName: process.env.NEXT_PUBLIC_SITE_NAME || "Gia Phả OS",
  exampleEmail: process.env.NEXT_PUBLIC_EXAMPLE_EMAIL || "",
  examplePassword: process.env.NEXT_PUBLIC_EXAMPLE_PASSWORD || "",
  demoDomain: process.env.NEXT_PUBLIC_DEMO_DOMAIN || "giapha-os.homielab.com",
};

export default config;
